#include <stdio.h>

int main()
{
    short int count1;
    long int count2;
    short count3;

    printf("short is %u bytes\n", sizeof(short int));
    printf("long is %u bytes\n", sizeof(int long));
    printf("short is %u bytes\n", sizeof(short));

    return 0;
}
